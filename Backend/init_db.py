# IMPORTANT:
# Supabase tables already exist. Do not run Base.metadata.create_all() against
# this project because the database schema is managed in Supabase.

print("Database schema is managed in Supabase. No local table creation required.")
